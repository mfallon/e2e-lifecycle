/*
import glob from 'glob';

export default class {
  constuctor() {
    this.files = glob(
      'content/*.json',
      {},
      this.loaded
    );
  }

  loaded(args) {
    console.log(this.files);
  }

}
*/
